{
  "name": "podium-protocol",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "deploy": "ts-node --project tsconfig.json scripts/deploy_all.ts",
    "deploy:cheer": "ts-node --project tsconfig.json scripts/deploy_cheer.ts",
    "deploy:dev": "ts-node --project tsconfig.json scripts/deploy_all.ts --dev",
    "deploy:cheer:dev": "ts-node --project tsconfig.json scripts/deploy_cheer.ts --dev",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aptos-labs/ts-sdk": "latest",
    "eventemitter3": "^5.0.1",
    "yaml": "^2.3.4"
  }
}
