{
  "type": "module",
  "scripts": {
    "deploy": "npx tsx scripts/deploy.ts",
    "deploy:dev": "npx tsx scripts/deploy.ts --dev",
    "deploy:test": "npx tsx scripts/test_deployment.ts",
    "deploy:dry-run": "npx tsx scripts/deploy.ts --dry-run"
  },
  "dependencies": {
    "@aptos-labs/ts-sdk": "latest",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2",
    "tsx": "^4.7.0"
  }
}
